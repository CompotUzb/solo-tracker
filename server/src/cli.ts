import fs from'node:fs';import path from'node:path';import{loadConfig,publicConfig}from'./config.js';import{migrate,openDatabase}from'./db.js';
async function main(){const command=process.argv[2]??'doctor';const config=loadConfig();if(command==='migrate'){migrate(config);console.log(`migrations applied to ${config.databasePath}`);return}if(command==='doctor'){console.log(JSON.stringify({ok:true,config:publicConfig(config),databaseDirectoryExists:fs.existsSync(path.dirname(path.resolve(config.databasePath))),discordLogin:config.skipDiscordLogin?'skipped':'enabled',tokenLoaded:Boolean(config.discordToken)},null,2));return}if(command==='export-json'){const db=openDatabase(config.databasePath);try{console.log(JSON.stringify({exportedAt:new Date().toISOString(),boundaries:publicConfig(config),dailyStats:db.prepare('select * from daily_stats order by local_date desc').all(),rankSnapshots:db.prepare('select * from rank_snapshots order by updated_at desc').all()},null,2))}finally{db.close()}return}throw new Error(`Unknown command: ${command}`)}main().catch(e=>{console.error(e instanceof Error?e.message:e);process.exitCode=1});
