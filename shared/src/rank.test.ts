import{describe,expect,it}from'vitest';import{computeRankState,xpRequiredForLevel}from'./index.js';
describe('rank rules',()=>{it('maps total XP to level boundaries',()=>{expect(computeRankState(0).level).toBe(1);expect(computeRankState(99).level).toBe(1);expect(computeRankState(100).level).toBe(2);expect(computeRankState(300).level).toBe(3);expect(computeRankState(600).level).toBe(4)});it('computes progress to next level',()=>{const s=computeRankState(340);expect(s.level).toBe(3);expect(s.rankCode).toBe('apprentice');expect(s.xpIntoLevel).toBe(40);expect(s.xpForNextLevel).toBe(300)});it('uses closed form XP requirement',()=>{expect(xpRequiredForLevel(1)).toBe(0);expect(xpRequiredForLevel(4)).toBe(600)})});
