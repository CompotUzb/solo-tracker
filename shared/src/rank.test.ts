import{describe,expect,it}from'vitest';import{computeRankState,computeStatLevel,xpRequiredForLevel}from'./index.js';
describe('stat level rules',()=>{it('levels each attribute on a gentle cumulative curve',()=>{expect(computeStatLevel(0).level).toBe(1);expect(computeStatLevel(4).level).toBe(1);expect(computeStatLevel(5).level).toBe(2);expect(computeStatLevel(14).level).toBe(2);expect(computeStatLevel(15).level).toBe(3);expect(computeStatLevel(30).level).toBe(4)});it('reports progress toward the next attribute level',()=>{const s=computeStatLevel(8);expect(s.level).toBe(2);expect(s.pointsIntoLevel).toBe(3);expect(s.pointsForNextLevel).toBe(10)});it('floors negative or non-finite values at level 1',()=>{expect(computeStatLevel(-3).level).toBe(1);expect(computeStatLevel(Number.NaN).level).toBe(1)})});
describe('rank rules',()=>{it('maps total XP to level boundaries',()=>{expect(computeRankState(0).level).toBe(1);expect(computeRankState(99).level).toBe(1);expect(computeRankState(100).level).toBe(2);expect(computeRankState(300).level).toBe(3);expect(computeRankState(600).level).toBe(4)});it('computes progress to next level',()=>{const s=computeRankState(340);expect(s.level).toBe(3);expect(s.rankCode).toBe('apprentice');expect(s.xpIntoLevel).toBe(40);expect(s.xpForNextLevel).toBe(300)});it('uses closed form XP requirement',()=>{expect(xpRequiredForLevel(1)).toBe(0);expect(xpRequiredForLevel(4)).toBe(600)})});
